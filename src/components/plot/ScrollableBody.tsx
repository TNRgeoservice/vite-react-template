// ════════════════════════════════════════
// components/plot/ScrollableBody.tsx
// DEPRECATED — body scroll-able by default แล้ว
// คงไฟล์ไว้เพื่อ backward-compat (component no-op)
// ใช้ <BodyFixed /> แทนถ้าต้องการ lock viewport (เช่นหน้าแผนที่)
// ════════════════════════════════════════
'use client';

export function ScrollableBody() {
  return null;
}
